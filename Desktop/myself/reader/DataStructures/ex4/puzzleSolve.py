# R45
# 414

def puzzleSolve(k, s, u):
    """

    :param k: int
    :param s: sequence
    :param u: set
    :return:
    """
    for i in u:
        s.append(i)
        u.pop(i)  # pop参数是索引还是值
        if k == 1:


def puzzleSolve_interative(s):
    